name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: make mupdf
      run: cd libs && make -f Makefile.mupdf && cd ..
    - name: make
      run: make
